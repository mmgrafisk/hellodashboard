{
  "name": "firebase-seed-cjs",
  "private": true,
  "version": "1.0.0",
  "description": "CommonJS Firestore seeding setup reading seeds/seed.data.json",
  "main": "seed.js",
  "scripts": {
    "seed": "node seed.js",
    "deploy:rules": "firebase deploy --only firestore:rules"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.31.2"
  }
}